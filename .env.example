# Example environment variables for Wingman (DO NOT put real secrets here)

# Gemini (optional). If not set, the app will fall back to a local mock response.
REACT_APP_GEMINI_API_KEY=
REACT_APP_GEMINI_MODEL=gemini-1.5-flash
